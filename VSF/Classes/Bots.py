from random import *
import pymysql
class Bots():
    def __init__(self, upperRisk, lowerRisk, buyRisk, numer):
        self.u = upperRisk
        self.l = lowerRisk
        self.b = buyRisk
        self.nr = numer
    def newbot(self):
        name = "Bot%d" %(self.nr)
        if nr not in bot:
            bot = self.nr, name, self.u, self.l, self.b, 10000
            return "skráning tókst"
        else:
            return "Bot með þetta númer er þegar til"
    def botbuys(self, nr):
        where nr in gagnagrunnur:
            if self.u

